# Configuration file for building bristlecone release

# Where build projects will end up. 
BUILD_DIR=build

# Version name of the release to build
VERSION_MAJOR=5
VERSION_MINOR=0
VERSION_REVISION=0
VERSION=$VERSION_MAJOR.$VERSION_MINOR.$VERSION_REVISION

# Set to 1 to use existing code (found in $BUILD_DIR).  This is now the default
# for Git. 
SKIP_CHECKOUT=1

# Set to 1 to skip the build.
SKIP_BUILD=0

# Set to 1 to skip source generation.
SKIP_SOURCE=1
